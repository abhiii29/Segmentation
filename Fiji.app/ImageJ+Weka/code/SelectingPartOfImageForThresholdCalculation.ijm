a=getImageID();
print(a);
/*
selectWindow("1400-1.tif");
width, height, channels, slices, frames=getDimensions(width, height, channels, slices, frames);
x_loc=0;
y_loc=0;
makeRectangle(x_loc, y_loc, width, height);
x+=
